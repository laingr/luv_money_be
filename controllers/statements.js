"use strict";

const auth = require("./authcontroller");
const models = require("../models");
const db = require("../db");

///// needs a service that runs this function once a day!

exports.statementGenerator = () => {
  
}